s.uniformsplit.UniformSplit.
 *
 * @param regex The regular expression to split the string on.
 * @param string The string to split.
 * @return A list of strings.

* The uniformsplit class is a wrapper around the Java class
 * org.apache.lucene.codecs.uniformsplit.UniformSplit.
 *
 * @param regex The regular expression to split the string on.
 * @param string The string to split.
 * @return A list of strings.

* The uniformsplit class is a wrapper around the Java class
 * org.apache.lucene.codecs.uniformsplit.UniformSplit.
 *
 * @param regex The regular expression to split the string on.
 * @param string The string to split.
 * @return A list of strings.

* The uniformsplit class is a wrapper around the Java class
 * org.apache.lucene.codecs.uniformsplit.UniformSplit.
 *
 * @param regex The regular expression to split the string on.
 * @param string The string to split.
 * @return A list of strings.

* The uniformsplit class is a wrapper around the Java class
 * org.apache.lucene.codecs.uniformsplit.UniformSplit.
 *
 * @param regex The regular expression to split the string on.
 * @param string The string to split.
 * @return A list of strings.

* The uniformsplit class is a wrapper around the Java class
 * org.apache.lucene.codecs.uniformsplit.UniformSplit.
 *
 * @param regex The regular expression to split the string on.
 * @param string The string to split.
 * @return A list of strings.

* The uniformsplit class is a wrapper around the Java class
 * org.apache.lucene.codecs.uniformsplit.UniformSplit.
 *
 * @param regex The regular expression to split the string on.
 * @param string The string to split.
 * @return A list of strings.

* The uniformsplit class is a wrapper around the Java class
 * org.apache.lucene.codecs.uniformsplit.UniformSplit.
 *
 * @param regex The regular expression to split the string on.
 * @param string The string to split.
 * @return A list of strings.

* The uniformsplit class is a wrapper around the Java class
 * org.apache.lucene.codecs.uniformsplit.UniformSplit.
 *
 * @param regex The regular expression to split the string on.
 * @param string The string to split.
 * @return A list of strings.

* The uniformsplit class is a wrapper around the Java class
 * org.apache.lucene.codecs.uniformsplit.UniformSplit.